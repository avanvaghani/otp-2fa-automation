name: Playwright OTP Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Create .env file
        run: |
          echo "TOTP_SECRET=JBSWY3DPEHPK3PXP" >> .env
          echo "DEMO_APP_PORT=3333" >> .env

      - name: Start demo app and wait for server
        run: |
          npm run start:app &
          sleep 3
          curl --retry 5 --retry-delay 2 --retry-connrefused http://localhost:3333 > /dev/null 2>&1

      - name: Run Playwright tests
        run: npm test

      - name: Upload test report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30
